name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Install quality tools
        run: |
          python -m pip install --upgrade pip
          pip install black flake8 isort mypy ruff
      - name: Ruff
        run: ruff check .
      - name: Flake8
        run: flake8 dataexcept tests
      - name: Black
        run: black --check --diff dataexcept tests examples
      - name: isort
        run: isort --check-only --diff dataexcept tests examples
      - name: mypy
        run: mypy dataexcept

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12', '3.13']
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest coverage
      - name: Test with coverage
        run: |
          coverage run -m pytest -q
          mv .coverage .coverage.${{ matrix.python-version }}
      - uses: actions/upload-artifact@v7
        with:
          name: coverage-${{ matrix.python-version }}
          path: .coverage.${{ matrix.python-version }}
          include-hidden-files: true
          if-no-files-found: error

  coverage:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - uses: actions/download-artifact@v8
        with:
          pattern: coverage-*
          path: coverage_files
          merge-multiple: true
      - name: Combine coverage
        run: |
          python -m pip install coverage coverage-badge "setuptools<81"
          coverage combine coverage_files
          coverage xml -o coverage.xml
          coverage html -d htmlcov
          coverage-badge -o coverage.svg -f
      - uses: actions/upload-artifact@v7
        with:
          name: coverage-report
          path: |
            coverage.xml
            coverage.svg
            htmlcov
          if-no-files-found: error

  docs:
    needs: [test, coverage]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Install documentation dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r docs/requirements.txt
      - name: Build documentation
        run: mkdocs build --strict
      - uses: actions/download-artifact@v8
        with:
          name: coverage-report
          path: coverage_report
      - name: Publish the coverage badge and HTML report with the docs
        run: |
          cp coverage_report/coverage.svg site/coverage.svg
          cp -r coverage_report/htmlcov site/htmlcov
      - uses: actions/upload-pages-artifact@v5
        with:
          path: site

  deploy-docs:
    needs: docs
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    # Let a running deployment finish rather than publishing half a site.
    concurrency:
      group: pages
      cancel-in-progress: false
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
